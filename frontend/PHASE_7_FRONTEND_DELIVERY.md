# Phase 7 前端交付记录

日期：2026-07-22

状态：`INTEGRATED / UI_READY / MOCK_READY / NOT_VERIFIED`

## 已完成

- Attendance 独立领域模型及统一导出。
- 集中式后端适配器，隔离 OpenAPI DTO、snake_case、数字 ID、分页包装和统计响应。
- 契约一致的 MSW fixtures/handlers，覆盖列表、详情、签到、批量签到、请假、自动生成和统计。
- 管理端、训练师端、学员端考勤入口；员工可按排课筛选、自动生成、批量签到、单人签到和标记请假。
- 学员仅查看本人考勤及个人统计；员工可查询排课统计。
- 管理端、训练师端、学员端路由与导航聚合。
- 适配器测试覆盖 DTO 映射、操作请求体、分页和两类统计响应。

## 自动化证据

- `npm.cmd run check:all`：通过。
- TypeScript、ESLint、Prettier：通过。
- 模块边界：269 个源文件通过。
- Vitest：36 个测试文件、121 项测试通过。
- 生产构建：612 个模块转换成功。

## 真实 API 证据

- 正式 Phase 7 数据库迁移和演示数据初始化成功。
- 按排课自动生成考勤成功；对同一排课重复调用保持幂等。
- 批量签到成功，单条标记请假成功。
- 学员账号只能读取本人考勤和本人统计。
- 排课维度统计返回成功。
- 跨租户训练师读取考勤详情返回 404，符合资源隐藏与租户隔离策略。
- 联调产生的临时排课、课程模板和教室均已通过公开 API 删除。

## 边界与遗留验收

- 本阶段未修改 `backend/**` 或 `contracts/**`。
- 考勤请求中的 `student_id` 按正式实现指向 User ID；该差异被封装在适配器和领域边界内，页面不解析后端 DTO。
- 当前页面以排课 ID 作为员工筛选入口；后续可用独立排课选择器改善体验，不影响现有接口分层。
- 尚未完成五角色浏览器端完整矩阵、异常网络矩阵和生产环境验收，因此如实标记 `NOT_VERIFIED`，不得标记为完全 `VERIFIED`。

Phase 7 的核心前端路径、Mock、自动化门禁和真实 API 联调均已完成，前端可以进入 Phase 8。
