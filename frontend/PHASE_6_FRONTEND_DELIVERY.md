# Phase 6 前端交付记录

日期：2026-07-22

状态：`INTEGRATED / UI_READY / MOCK_READY / PARTIALLY_BLOCKED`

## 已完成

- Schedule、RecurringRule、Booking 独立领域模型。
- schedules/bookings 集中后端适配器，处理数字 ID、分页、嵌套摘要、snake_case 和单次/周期联合响应。
- 正式契约 MSW fixtures/handlers，覆盖列表、详情、创建、PATCH、删除、预约和取消。
- 排课列表、日期和状态筛选、单次/周期排课表单、详情编辑、删除、预约人数。
- 员工通过可见学员选择器代预约，不要求手填内部 ID。
- 管理端、教练端和学员端预约列表与取消。
- 管理端、教练端、学员端路由和导航入口。
- 适配器测试覆盖映射、单次/周期联合响应、PATCH、员工代预约、学员自助请求体和取消。

## 自动化证据

- TypeScript、ESLint、Prettier：通过。
- 模块边界：253 个源文件通过。
- Vitest：35 个测试文件、118 项测试通过。
- 生产构建：592 个模块转换成功。

## 真实 API 证据

- 正式 Phase 6–7 数据库迁移成功，Django system check 无错误。
- 单次排课创建成功；相同房间/教练/开始时间的重复排课返回 409。
- 员工代预约与取消成功。
- 周期排课按 ISO weekday 和两周规则创建 2 条。
- 次卡预约从 3 次扣减到 2 次，首次取消恢复为 3 次。
- 联调后恢复演示学员原始月卡，删除临时排课、课程模板和教室。

## 阻塞项

- `BE-06-01`：学员可以调用自助预约动作，但没有学员可访问的可预约课程发现端点。因此学员目前可以查看和取消自己的预约，不能从课程市场选择新排课。
- `BE-06-02`：预约请求 `student_id` 实际引用 User ID，不是 StudentProfile ID；前端已通过学员选择器提交 `student.user.id`，契约仍应补充资源语义。

除学员课程发现入口外，Phase 6 的员工排课、代预约和预约管理核心路径已经完成。不得在 `BE-06-01` 冻结前把 Phase 6 标记为完全 `VERIFIED`。
