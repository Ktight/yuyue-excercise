/**
 * 运行时配置公开出口。
 *
 * 页面和组件应从此处导入：
 *   import { config } from '@/shared/config';
 */
export { config, createRuntimeConfig } from './runtime-config';
export type { AppMode, RuntimeConfig, RuntimeConfigInput } from './runtime-config';
