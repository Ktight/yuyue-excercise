import { bookingsAdapter } from '@/backend-adapters';
export const fetchBookings = bookingsAdapter.fetchBookings;
export const fetchBooking = bookingsAdapter.fetchBooking;
export const createBooking = bookingsAdapter.createBooking;
export const bookSchedule = bookingsAdapter.bookSchedule;
export const cancelBooking = bookingsAdapter.cancelBooking;
