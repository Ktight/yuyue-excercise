import { schedulesAdapter } from '@/backend-adapters';
export const fetchSchedules = schedulesAdapter.fetchSchedules;
export const fetchSchedule = schedulesAdapter.fetchSchedule;
export const createSchedule = schedulesAdapter.createSchedule;
export const updateSchedule = schedulesAdapter.updateSchedule;
export const deleteSchedule = schedulesAdapter.deleteSchedule;
