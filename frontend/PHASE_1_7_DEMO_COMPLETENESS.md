# Phase 1–7 前端演示完整性记录

更新日期：2026-07-22

## 本轮补齐

- Phase 1：个人资料页面；退出登录调用后端撤销接口，同时确保失败时仍清理本地会话。
- Phase 2：用户详情、编辑姓名/角色/门店、启停账号、重置密码、列表点击、账号状态展示与筛选。
- Phase 3：公司和门店列表增加搜索、状态筛选和分页。
- Phase 4：课程模板详情增加受控删除入口、二次确认和关联冲突反馈。
- Phase 5：学员列表增加账号状态、会员状态筛选和分页。
- Phase 6：排课表单把门店、教室、课程模板、教练内部 ID 改为可读选择器；排课和预约列表增加分页，预约增加状态筛选；增加学员“发现课程并预约”演示页。
- Phase 7：考勤的手填排课 ID 改为排课选择器；增加考勤分页。
- 公共能力：新增独立 `AppPagination.vue`，避免每个业务页面重复维护分页逻辑。

## 演示边界

- `VITE_ENABLE_MOCK=true`：用于确认页面数量、导航、表单、列表、操作反馈和完整演示路径。
- 真实 API 模式：用于最后的契约和后端行为验证。
- 学员课程发现仍依赖 BE-FE-06；演示完成不等于真实接口已完成。
- 本轮没有凭空修改后端或契约，也没有把 Mock 数据写入页面。

## 状态定义

当前状态：`DEMO_UI_COMPLETE / MOCK_INTEGRATION_READY / REAL_API_NOT_FULLY_VERIFIED`。

最终验证：`check:all` 通过，282 个源文件边界检查通过，36 个测试文件共 125 项测试通过，生产构建转换 643 个模块；Playwright E2E 通过。真实后端浏览器检查已确认用户列表可以进入用户详情，详情页显示资料编辑、门店调整、启停和密码重置入口，本轮未提交任何会改变测试数据的表单。

只有 `BACKEND_DEPENDENCIES_PHASE_1_7.md` 中所有需要真实验证的条目完成后，Phase 1–7 才能整体提升为 `VERIFIED`。
